# Dependensi TAMBAHAN untuk jalur dataset nyata PhysioNet.
# JANGAN dipasang saat sesi hari-H. Sesi memakai dataset sintetis di data/.
# Pasang hanya jika kamu mengerjakan bonus KPP atau eksplorasi mandiri:
#
#     pip install -r requirements-physionet.txt
#
# CATATAN (diperbaiki setelah uji langsung): wfdb 4.1.0 mensyaratkan
# pandas<2.0.0, bentrok dengan pandas==2.2.2 yang dipatok di requirements.txt.
# Akibatnya "pip install -r requirements-physionet.txt" gagal / mencoba
# membangun pandas versi lama dari source dan error.
#
# wfdb 4.1.2 mensyaratkan pandas>=1.3.0 (tanpa batas atas) sehingga
# terpasang bersih berdampingan dengan pandas==2.2.2 -- sudah diuji langsung
# (install sukses, tidak menurunkan/menaikkan versi pandas yang sudah ada).
# wfdb 4.2.0 ke atas mensyaratkan pandas>=2.2.3, sedikit di atas pin kita,
# jadi TIDAK dipakai supaya tidak memaksa pandas naik versi.
wfdb==4.1.2
